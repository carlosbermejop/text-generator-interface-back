# Text Generator Interface - Backend

Backend broker to handle requests to GPT-3 that makes sure requests are kept at a reasonable rate, responses are kept and files are created to output the results.